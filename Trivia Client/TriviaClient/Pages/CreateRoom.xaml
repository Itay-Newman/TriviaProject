<Page x:Class="TriviaClient.Pages.CreateRoom"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TriviaClient.Pages"
      xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
      mc:Ignorable="d" 
      d:DesignHeight="410" d:DesignWidth="540"
      Title="Create Room">

    <Grid Background="Transparent">
        <!-- Foreground UI -->
        <Grid Panel.ZIndex="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Title -->
            <TextBlock Text="Create Room" FontSize="36" FontWeight="Bold" Foreground="White" 
                       HorizontalAlignment="Center" Margin="0,15"/>

            <!-- Form with dark background -->
            <Border Grid.Row="1" Background="#AA000000" CornerRadius="10" Padding="20" Margin="0,5,0,40" HorizontalAlignment="Center" Width="320">
                <StackPanel>
                    <!-- Stylish Button Style -->
                    <StackPanel.Resources>
                        <Style x:Key="ModernButtonStyle" TargetType="Button">
                            <Setter Property="Background" Value="#FF3A86FF"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="FontSize" Value="16"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Setter Property="Margin" Value="0,8,0,0"/>
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Effect">
                                <Setter.Value>
                                    <DropShadowEffect Color="#66000000" BlurRadius="8" ShadowDepth="2"/>
                                </Setter.Value>
                            </Setter>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                SnapsToDevicePixels="True">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#FF265DAB"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#FF1B3A6B"/>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Background" Value="#FFB0B0B0"/>
                                    <Setter Property="Foreground" Value="#FFEEEEEE"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Resources>

                    <xctk:WatermarkTextBox x:Name="RoomName" Height="30" FontSize="15" Margin="0,10" Watermark="Room Name"/>

                    <xctk:WatermarkTextBox x:Name="TimePerQuestion" Watermark="Time per question" Height="30" FontSize="15" Margin="0,0,0,10" PreviewTextInput="NumericOnly_PreviewTextInput" />

                    <xctk:WatermarkTextBox x:Name="MaxPlayers" Watermark="Max players" Height="30" FontSize="15" Margin="0,0,0,10" PreviewTextInput="NumericOnly_PreviewTextInput" />

                    <xctk:WatermarkTextBox x:Name="QuestionNumbers" Watermark="Question numbers" Height="30" FontSize="15" Margin="0,0,0,10" PreviewTextInput="NumericOnly_PreviewTextInput" />

                    <Button Content="Create" Height="45" FontSize="20" Margin="0,5,0,0" Click="CreateRoom_Click" Style="{StaticResource ModernButtonStyle}"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Page>
