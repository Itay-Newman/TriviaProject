<Page x:Class="TriviaClient.Pages.Records"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TriviaClient.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="410" d:DesignWidth="540"
      Title="Records">

    <Grid Background="Transparent">
        <Border Background="#AA000000" CornerRadius="10" Padding="20" Width="400" HorizontalAlignment="Center" VerticalAlignment="Center" Height="300">
            <StackPanel>
                <TextBlock Text="Leaderboard" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20" HorizontalAlignment="Center"/>

                <DataGrid x:Name="LeaderBoardGrid"
                          AutoGenerateColumns="False"
                          HeadersVisibility="Column"
                          CanUserResizeRows="False"
                          CanUserResizeColumns="False"
                          CanUserSortColumns="False"
                          GridLinesVisibility="None"
                          RowBackground="#222222"
                          AlternatingRowBackground="#333333"
                          Background="#AA000000"
                          Foreground="BLACK"
                          BorderThickness="0"
                          RowHeight="30"
                          FontSize="14"
                          IsReadOnly="True"
                          HorizontalScrollBarVisibility="Disabled"
                          VerticalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding Rank}" Width="40"/>
                        <DataGridTextColumn Header="Username" Binding="{Binding Username}" Width="*"/>
                        <DataGridTextColumn Header="Score" Binding="{Binding Score}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </StackPanel>
        </Border>
    </Grid>
</Page>
